// isComment
package isPackage;

import static isImport;
import static isImport;
import static isImport;
import isImport;
import isImport;
import isImport;
import isImport;
import isImport;
import isImport;
import isImport;
import isImport;
import isImport;
import isImport;
import isImport;

public class isClassOrIsInterface extends isClassOrInterfaceInstantiation {

    /**
     * isComment
     */
    public static void isMethod() throws isClassOrInterfaceInstantiation {
        isClassOrInterfaceInstantiation isVariable = new isClassOrInterfaceInstantiation();
        isClassOrInterfaceInstantiation isVariable = new isClassOrInterfaceInstantiation(isNameExpr);
        isClassOrInterfaceInstantiation isVariable = isNameExpr.isMethod();
        isClassOrInterfaceInstantiation isVariable = new isClassOrInterfaceInstantiation();
        isClassOrInterfaceInstantiation isVariable = new isClassOrInterfaceInstantiation(isNameExpr);
        isClassOrInterfaceInstantiation[] isVariable = isNameExpr.isMethod();
        for (isClassOrInterfaceInstantiation isVariable : isNameExpr) {
            isClassOrInterfaceInstantiation isVariable = isNameExpr.isMethod(isClassOrInterfaceInstantiation.class);
            isClassOrInterfaceInstantiation<? extends isClassOrInterfaceInstantiation> isVariable = isNameExpr.isMethod(isNameExpr);
            if (isNameExpr != null) {
                isMethod(isNameExpr.isMethod(), isNameExpr);
            } else {
                isMethod(isNameExpr);
            }
        }
    }

    public static class isClassOrIsInterface {

        @Match(isClassOrInterfaceInstantiation.class)
        public isClassOrInterfaceInstantiation isMethod() {
            return null;
        }

        public isClassOrInterfaceInstantiation isMethod() {
            return null;
        }

        @Match(isClassOrInterfaceInstantiation.class)
        public isClassOrInterfaceInstantiation isMethod() {
            return null;
        }
    }

    @Retention(isNameExpr)
    @Target({ isNameExpr, isNameExpr })
    public @interface Match {

        isClassOrInterfaceInstantiation<? extends isClassOrInterfaceInstantiation> value();
    }
}
