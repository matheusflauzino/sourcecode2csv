// isComment
package isPackage;

import static isImport;
import static isImport;
import static isImport;
import static isImport;
import isImport;
import isImport;
import isImport;

@Retention(isNameExpr)
@Target({ isNameExpr, isNameExpr, isNameExpr })
@Convention("isStringConstant")
public @interface TextType {

    isClassOrInterfaceInstantiation.isClassOrInterfaceInstantiation value() default isNameExpr.isFieldAccessExpr.isFieldAccessExpr;
}
