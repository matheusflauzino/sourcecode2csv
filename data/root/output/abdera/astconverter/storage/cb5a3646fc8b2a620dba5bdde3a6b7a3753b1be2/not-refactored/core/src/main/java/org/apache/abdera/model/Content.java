// isComment
package isPackage;

import isImport;
import isImport;
import isImport;
import isImport;
import isImport;
import isImport;

/**
 * isComment
 */
public interface isClassOrIsInterface extends isClassOrInterfaceInstantiation {

    public enum Type {

        TEXT, HTML, XHTML, XML, MEDIA;

        public static isClassOrInterfaceInstantiation isMethod(isClassOrInterfaceInstantiation isParameter) {
            isClassOrInterfaceInstantiation isVariable = isNameExpr;
            if (isNameExpr != null) {
                if (isNameExpr.isMethod("isStringConstant"))
                    isNameExpr = isNameExpr;
                else if (isNameExpr.isMethod("isStringConstant"))
                    isNameExpr = isNameExpr;
                else if (isNameExpr.isMethod("isStringConstant"))
                    isNameExpr = isNameExpr;
                else if (isNameExpr.isMethod(isNameExpr))
                    isNameExpr = isNameExpr;
                else {
                    isNameExpr = isNameExpr.isMethod(isNameExpr) ? isNameExpr : null;
                }
            }
            return isNameExpr;
        }
    }

    isClassOrInterfaceInstantiation isMethod();

    <T extends isClassOrInterfaceInstantiation> isClassOrInterfaceInstantiation isMethod();

    <T extends isClassOrInterfaceInstantiation> void isMethod(isClassOrInterfaceInstantiation isParameter);

    /**
     * isComment
     */
    isClassOrInterfaceInstantiation isMethod();

    /**
     * isComment
     */
    void isMethod(isClassOrInterfaceInstantiation isParameter);

    /**
     * isComment
     */
    void isMethod(isClassOrInterfaceInstantiation isParameter);

    /**
     * isComment
     */
    isClassOrInterfaceInstantiation isMethod() throws isClassOrInterfaceInstantiation;

    /**
     * isComment
     */
    isClassOrInterfaceInstantiation isMethod() throws isClassOrInterfaceInstantiation;

    /**
     * isComment
     */
    void isMethod(isClassOrInterfaceInstantiation isParameter);

    /**
     * isComment
     */
    void isMethod(isClassOrInterfaceInstantiation isParameter) throws isClassOrInterfaceInstantiation;

    /**
     * isComment
     */
    isClassOrInterfaceInstantiation isMethod();

    /**
     * isComment
     */
    void isMethod(isClassOrInterfaceInstantiation isParameter);
}
