// isComment
package isPackage;

import static isImport;
import static isImport;
import static isImport;
import isImport;
import isImport;

@Retention(isNameExpr)
@Target({ isNameExpr, isNameExpr })
public @interface VarName {

    isClassOrInterfaceInstantiation value();
}
