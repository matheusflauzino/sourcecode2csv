// isComment
package isPackage;

import isImport;
import isImport;

public class isClassOrIsInterface extends isClassOrInterfaceInstantiation {

    public isConstructor() {
        super();
        // isComment
        isMethod(isClassOrInterfaceInstantiation.class);
        isMethod(isClassOrInterfaceInstantiation.class);
        isMethod(isClassOrInterfaceInstantiation.class);
        isMethod(isClassOrInterfaceInstantiation.class);
        isMethod(isClassOrInterfaceInstantiation.class);
        isMethod(isClassOrInterfaceInstantiation.class);
        isMethod(isClassOrInterfaceInstantiation.class);
        isMethod(isClassOrInterfaceInstantiation.class);
        isMethod(isClassOrInterfaceInstantiation.class);
        isMethod(isClassOrInterfaceInstantiation.class);
        // isComment
        isClassOrInterfaceInstantiation isVariable = isNameExpr.isMethod("isStringConstant");
        if (isNameExpr.isMethod() < isIntegerConstant)
            throw new isClassOrInterfaceInstantiation("isStringConstant" + isNameExpr);
        int isVariable = isNameExpr.isMethod(isIntegerConstant) - 'isStringConstant';
        if (isNameExpr < isIntegerConstant || isNameExpr > isIntegerConstant)
            throw new isClassOrInterfaceInstantiation("isStringConstant" + isNameExpr);
        if (isNameExpr < isIntegerConstant) {
            // isComment
            isMethod(isClassOrInterfaceInstantiation.class);
            if (isNameExpr > isIntegerConstant) {
                // isComment
                isMethod(isClassOrInterfaceInstantiation.class);
                // isComment
                isMethod(isClassOrInterfaceInstantiation.class);
            }
        }
        if (isNameExpr > isIntegerConstant) {
            // isComment
            isMethod(isClassOrInterfaceInstantiation.class);
            // isComment
            isMethod(isClassOrInterfaceInstantiation.class);
        }
        // isComment
        if (isNameExpr > isIntegerConstant) {
            // isComment
            isMethod(isClassOrInterfaceInstantiation.class);
        }
        if (isNameExpr > isIntegerConstant) {
            // isComment
            isMethod(isClassOrInterfaceInstantiation.class);
        }
    }

    public static isClassOrInterfaceInstantiation isMethod() {
        return new isClassOrInterfaceInstantiation();
    }

    public static void isMethod(isClassOrInterfaceInstantiation[] isParameter) {
        isNameExpr.isMethod(isMethod());
    }
}
