// isComment
package isPackage;

import isImport;
import isImport;
import isImport;

/**
 * isComment
 */
public final class isClassOrIsInterface {

    private isConstructor() {
    }

    /**
     * isComment
     */
    public static isClassOrInterfaceInstantiation isMethod(isClassOrInterfaceInstantiation isParameter) {
        if (isNameExpr == null) {
            throw new isClassOrInterfaceInstantiation();
        }
        if (isNameExpr instanceof isClassOrInterfaceInstantiation) {
            // isComment
            return ((isClassOrInterfaceInstantiation) isNameExpr).isMethod();
        }
        if (isNameExpr instanceof isClassOrInterfaceInstantiation) {
            // isComment
            return ((isClassOrInterfaceInstantiation) isNameExpr).isMethod();
        }
        // isComment
        try {
            isClassOrInterfaceInstantiation isVariable = isNameExpr.isMethod().isMethod("isStringConstant", (isClassOrInterfaceInstantiation[]) null);
            if (isNameExpr != null)
                return (isClassOrInterfaceInstantiation) isNameExpr.isMethod(isNameExpr, (isClassOrInterfaceInstantiation[]) null);
        } catch (isClassOrInterfaceInstantiation isParameter) {
        }
        throw new isClassOrInterfaceInstantiation("isStringConstant" + isNameExpr.isMethod());
    }
}
