// isComment
package isPackage;

import isImport;
import isImport;
import isImport;

public abstract class isClassOrIsInterface<T> {

    private final isClassOrInterfaceInstantiation<isClassOrInterfaceInstantiation> isVariable = new isClassOrInterfaceInstantiation<isClassOrInterfaceInstantiation>();

    protected synchronized isClassOrInterfaceInstantiation isMethod(isClassOrInterfaceInstantiation isParameter) throws isClassOrInterfaceInstantiation {
        if (!isNameExpr.isMethod())
            return isNameExpr.isMethod();
        return isMethod(isNameExpr);
    }

    protected synchronized void isMethod(isClassOrInterfaceInstantiation isParameter) {
        if (isNameExpr == null || isNameExpr.isMethod(isNameExpr))
            return;
        isNameExpr.isMethod(isNameExpr);
    }

    protected abstract isClassOrInterfaceInstantiation isMethod(isClassOrInterfaceInstantiation isParameter) throws isClassOrInterfaceInstantiation;
}
