// isComment
package isPackage;

import isImport;
import isImport;
import isImport;
import isImport;
import isImport;

public class isClassOrIsInterface extends isClassOrInterfaceInstantiation implements isClassOrInterfaceInstantiation {

    private isClassOrInterfaceInstantiation isVariable = null;

    protected synchronized isClassOrInterfaceInstantiation isMethod(isClassOrInterfaceInstantiation isParameter) throws isClassOrInterfaceInstantiation {
        if (isNameExpr == null) {
            isNameExpr = new isClassOrInterfaceInstantiation(isNameExpr, this);
        }
        return isNameExpr;
    }
}
