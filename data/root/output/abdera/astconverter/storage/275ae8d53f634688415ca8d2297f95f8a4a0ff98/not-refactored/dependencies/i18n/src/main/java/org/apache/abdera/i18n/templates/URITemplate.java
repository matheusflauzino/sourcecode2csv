// isComment
package isPackage;

import static isImport;
import static isImport;
import isImport;
import isImport;

@Retention(isNameExpr)
@Target(isNameExpr)
public @interface URITemplate {

    isClassOrInterfaceInstantiation value();

    boolean isiri() default true;
}
