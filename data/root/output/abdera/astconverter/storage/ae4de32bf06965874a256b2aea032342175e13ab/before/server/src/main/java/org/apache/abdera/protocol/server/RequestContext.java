// isComment
package isPackage;

import isImport;
import isImport;
import isImport;
import isImport;
import isImport;
import isImport;
import isImport;
import isImport;
import isImport;
import isImport;
import isImport;
import isImport;
import isImport;

public interface isClassOrIsInterface extends isClassOrInterfaceInstantiation {

    public enum Scope {

        REQUEST, SESSION
    }

    public enum Property {

        SESSIONID,
        SESSIONCREATED,
        SESSIONACCESSED,
        SESSIONTIMEOUT,
        CHARACTERENCODING,
        LOCALES,
        PROTOCOL,
        REMOTEADDRESS,
        REMOTEHOST,
        REMOTEUSER,
        SCHEME,
        PRINCIPAL
    }

    isClassOrInterfaceInstantiation isMethod();

    isClassOrInterfaceInstantiation isMethod();

    isClassOrInterfaceInstantiation isMethod();

    isClassOrInterfaceInstantiation isMethod();

    isClassOrInterfaceInstantiation isMethod();

    isClassOrInterfaceInstantiation isMethod();

    isClassOrInterfaceInstantiation isMethod();

    isClassOrInterfaceInstantiation isMethod(isClassOrInterfaceInstantiation isParameter);

    isClassOrInterfaceInstantiation isMethod(isClassOrInterfaceInstantiation isParameter);

    isClassOrInterfaceInstantiation[] isMethod();

    isClassOrInterfaceInstantiation<isClassOrInterfaceInstantiation> isMethod(isClassOrInterfaceInstantiation isParameter);

    isClassOrInterfaceInstantiation isMethod(isClassOrInterfaceInstantiation isParameter, isClassOrInterfaceInstantiation isParameter);

    isClassOrInterfaceInstantiation[] isMethod(isClassOrInterfaceInstantiation isParameter);

    void isMethod(isClassOrInterfaceInstantiation isParameter, isClassOrInterfaceInstantiation isParameter, isClassOrInterfaceInstantiation isParameter);

    isClassOrInterfaceInstantiation isMethod() throws isClassOrInterfaceInstantiation;

    isClassOrInterfaceInstantiation isMethod() throws isClassOrInterfaceInstantiation;

    <T extends isClassOrInterfaceInstantiation> isClassOrInterfaceInstantiation<isClassOrInterfaceInstantiation> isMethod() throws isClassOrInterfaceInstantiation, isClassOrInterfaceInstantiation;

    <T extends isClassOrInterfaceInstantiation> isClassOrInterfaceInstantiation<isClassOrInterfaceInstantiation> isMethod(isClassOrInterfaceInstantiation isParameter) throws isClassOrInterfaceInstantiation, isClassOrInterfaceInstantiation;

    <T extends isClassOrInterfaceInstantiation> isClassOrInterfaceInstantiation<isClassOrInterfaceInstantiation> isMethod(isClassOrInterfaceInstantiation isParameter, isClassOrInterfaceInstantiation isParameter) throws isClassOrInterfaceInstantiation, isClassOrInterfaceInstantiation;

    <T extends isClassOrInterfaceInstantiation> isClassOrInterfaceInstantiation<isClassOrInterfaceInstantiation> isMethod(isClassOrInterfaceInstantiation isParameter) throws isClassOrInterfaceInstantiation, isClassOrInterfaceInstantiation;

    boolean isMethod(isClassOrInterfaceInstantiation isParameter);
}
