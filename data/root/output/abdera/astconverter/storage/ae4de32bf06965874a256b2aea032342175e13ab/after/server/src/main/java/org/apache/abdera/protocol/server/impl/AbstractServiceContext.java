// isComment
package isPackage;

import isImport;
import isImport;
import isImport;
import isImport;
import isImport;
import isImport;
import isImport;
import isImport;
import isImport;
import isImport;

public abstract class isClassOrIsInterface implements isClassOrInterfaceInstantiation {

    protected isClassOrInterfaceInstantiation isVariable;

    protected isClassOrInterfaceInstantiation<isClassOrInterfaceInstantiation, isClassOrInterfaceInstantiation> isVariable;

    protected isClassOrInterfaceInstantiation<isClassOrInterfaceInstantiation> isVariable;

    protected isClassOrInterfaceInstantiation<isClassOrInterfaceInstantiation> isVariable;

    protected isClassOrInterfaceInstantiation<isClassOrInterfaceInstantiation> isVariable;

    protected isClassOrInterfaceInstantiation<isClassOrInterfaceInstantiation> isVariable;

    public synchronized void isMethod(isClassOrInterfaceInstantiation isParameter, isClassOrInterfaceInstantiation<isClassOrInterfaceInstantiation, isClassOrInterfaceInstantiation> isParameter) {
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = (isNameExpr != null) ? isNameExpr : new isClassOrInterfaceInstantiation<isClassOrInterfaceInstantiation, isClassOrInterfaceInstantiation>();
    }

    public isClassOrInterfaceInstantiation isMethod() {
        return isNameExpr;
    }

    public isClassOrInterfaceInstantiation isMethod(isClassOrInterfaceInstantiation isParameter) {
        return isNameExpr.isMethod(isNameExpr);
    }

    public isClassOrInterfaceInstantiation[] isMethod() {
        return isNameExpr.isMethod().isMethod(new isClassOrInterfaceInstantiation[isNameExpr.isMethod()]);
    }
}
