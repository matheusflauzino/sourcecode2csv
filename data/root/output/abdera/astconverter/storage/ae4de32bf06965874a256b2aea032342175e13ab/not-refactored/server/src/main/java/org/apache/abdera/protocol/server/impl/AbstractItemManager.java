// isComment
package isPackage;

import isImport;
import isImport;
import isImport;

public abstract class isClassOrIsInterface<T> extends isClassOrInterfaceInstantiation<isClassOrInterfaceInstantiation> implements isClassOrInterfaceInstantiation<isClassOrInterfaceInstantiation> {

    public isClassOrInterfaceInstantiation isMethod(isClassOrInterfaceInstantiation isParameter) {
        return isMethod();
    }
}
