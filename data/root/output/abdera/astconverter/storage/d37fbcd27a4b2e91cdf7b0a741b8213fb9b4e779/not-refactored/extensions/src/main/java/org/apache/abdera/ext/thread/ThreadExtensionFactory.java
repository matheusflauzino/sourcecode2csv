// isComment
package isPackage;

import isImport;
import isImport;
import isImport;
import isImport;
import isImport;
import isImport;
import isImport;
import isImport;
import isImport;
import isImport;
import isImport;

public class isClassOrIsInterface implements isClassOrInterfaceInstantiation, isClassOrInterfaceInstantiation {

    public boolean isMethod(isClassOrInterfaceInstantiation isParameter) {
        return (isNameExpr.isFieldAccessExpr.isMethod(isNameExpr));
    }

    public isClassOrInterfaceInstantiation isMethod() {
        return isNameExpr.isFieldAccessExpr;
    }

    @SuppressWarnings("isStringConstant")
    public <T extends isClassOrInterfaceInstantiation> isClassOrInterfaceInstantiation isMethod(isClassOrInterfaceInstantiation isParameter, isClassOrInterfaceInstantiation isParameter, isClassOrInterfaceInstantiation isParameter) {
        if (isNameExpr.isFieldAccessExpr.isMethod(isNameExpr))
            return (isClassOrInterfaceInstantiation) new isClassOrInterfaceInstantiation(isNameExpr, (isClassOrInterfaceInstantiation) isNameExpr, (isClassOrInterfaceInstantiation) isNameExpr);
        else if (isNameExpr.isFieldAccessExpr.isMethod(isNameExpr))
            return (isClassOrInterfaceInstantiation) new isClassOrInterfaceInstantiation(isNameExpr, (isClassOrInterfaceInstantiation) isNameExpr, (isClassOrInterfaceInstantiation) isNameExpr);
        else
            return null;
    }

    @SuppressWarnings("isStringConstant")
    public <T extends isClassOrInterfaceInstantiation> isClassOrInterfaceInstantiation isMethod(isClassOrInterfaceInstantiation isParameter, isClassOrInterfaceInstantiation isParameter, isClassOrInterfaceInstantiation isParameter, isClassOrInterfaceInstantiation isParameter) {
        if (isNameExpr.isFieldAccessExpr.isMethod(isNameExpr))
            return (isClassOrInterfaceInstantiation) new isClassOrInterfaceInstantiation(isNameExpr, (isClassOrInterfaceInstantiation) isNameExpr, (isClassOrInterfaceInstantiation) isNameExpr, isNameExpr);
        else if (isNameExpr.isFieldAccessExpr.isMethod(isNameExpr))
            return (isClassOrInterfaceInstantiation) new isClassOrInterfaceInstantiation(isNameExpr, (isClassOrInterfaceInstantiation) isNameExpr, (isClassOrInterfaceInstantiation) isNameExpr, isNameExpr);
        else
            return null;
    }
}
